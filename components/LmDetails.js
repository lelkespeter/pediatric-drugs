import {StyleSheet, Text, View} from "react-native";
import React from "react";

const LmDetails = ({children}) => {
  return (
    <View>
      <Text>LmDetails</Text>
    </View>
  );
};

export default LmDetails;

const styles = StyleSheet.create({});
